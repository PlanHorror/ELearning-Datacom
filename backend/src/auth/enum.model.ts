// Purpose: Enum for user roles.
export enum Role {
  ADMIN = 'Admin',
  CUSTOMER = 'Customer',
  COMPANY = 'Company',
}

// Purpose: Enum for user status.
export enum Status {
  ACTIVE = 'Active',
  INACTIVE = 'Inactive',
  BANNED = 'Banned',
}

// Purpose: Enum for gender.
export enum Gender {
  MALE = 'Male',
  FEMALE = 'Female',
  OTHER = 'Other',
}
